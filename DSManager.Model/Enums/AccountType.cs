namespace DSManager.Model.Enums {
    public enum AccountType {
        Boss,           // Szef
        Coordinator,    // Koordynator
        Instructor,     // Instruktor
        Student         // Kursant
    }
}
